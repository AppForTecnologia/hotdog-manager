import React from 'react';

/**
 * Componente temporário para evitar erros
 * Será reativado gradualmente após resolver os problemas
 */
const MasterUserSetup = () => {
  // Por enquanto, apenas retorna null para evitar erros
  return null;
};

export default MasterUserSetup;
