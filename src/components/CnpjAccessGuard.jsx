import React from 'react';

/**
 * Componente temporário para evitar erros
 * Será reativado gradualmente após resolver os problemas
 */
const CnpjAccessGuard = ({ children, fallback = null }) => {
  // Por enquanto, apenas retorna os children para evitar erros
  return children;
};

export default CnpjAccessGuard;
